source "https://rubygems.org"

gem "jekyll", "~> 4.3"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
end

# Netlify's Ruby buildpack reads this file to install the exact
# Jekyll version needed — keeps the build consistent every time.
